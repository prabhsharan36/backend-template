// import { Router } from "express";
// import CostRepository from "../controllers/cost.controller";
// const router = Router();

// // Route::get('/cost/{country?}', 'CostListingController@listing');
// // 			Route::get('/cost-list/{category}/{country}/{city?}', 'CostCategoryPageController@listing');

// router.get("/:country", (req, res) => {
//   const costRepository: any = new CostRepository(req, res);
//   costRepository.listing(req, res);
// });

// router.get("/:cost/:category/:country/:city?", (req, res) => {
//   const costRepository: any = new CostRepository(req, res);
//   costRepository.listing;
// });
// export default router;
